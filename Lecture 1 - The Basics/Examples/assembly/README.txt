Example assembly files and make recipes

Requirements:
    Linux/WSL with:
        GCC, LD, MAKE
        NASM
	The gcc-multilib package to compile 32-bit code

Refer to makefile for targets, or simply make all